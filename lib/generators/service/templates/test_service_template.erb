# frozen_string_literal: true

require 'test_helper'

class <%= class_name + "Test" %> < ActiveSupport::TestCase
  describe '#call' do
    subject { <%= class_name %>.call!(**args) }
    let(:args) do
      {
      <%- attributes.each do |attr| -%>
        <%= attr.name %>: nil,
      <%- end -%>
      }
    end

    it 'works' do
      subject
    end
  end
end